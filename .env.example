# Base URL for underwritting_api - read in src/lib/api.js. Falls back to this same
# default if unset, so this file only needs to exist for a non-default API location.
VITE_API_URL=http://localhost:8000
